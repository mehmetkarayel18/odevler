import locale
locale.setlocale(locale.LC_ALL, "Turkish_Turkey.1254")  #Windows için
while True:
    yazı=input("Lütfen bir yazı yazınız....:")
    yazı=yazı.replace("i","İ")
            
    yazı=yazı.upper()
    print(yazı)
